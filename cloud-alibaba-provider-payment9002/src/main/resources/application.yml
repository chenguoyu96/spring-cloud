server:
  port: 9002

spring:
  profiles:
    active: common,mysql
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'